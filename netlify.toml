[build.environment]
  NPM_VERSION = "8.7.0"
  NODE_VERSION = "18.0.0"
  HUGO_VERSION = "0.91.2"
  
[build]
  command = "npm run build"
  publish = "dist"

[context.deploy-preview]
  command = "npm run build:preview"
